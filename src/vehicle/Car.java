package vehicle;

import engine.Engine;

/**
 * Car class to create a car object
 */
public class Car extends Vehicle {

    public Car(float fuelsCapacity, Engine engine) {
        super(fuelsCapacity, engine);
    }
}
